import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maestrosdeltaller.app',
  appName: 'Master of Taller',
  webDir: 'dist'
};

export default config;
